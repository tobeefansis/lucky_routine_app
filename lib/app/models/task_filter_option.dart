import 'dart:ui';

class TaskFilterOption {
  const TaskFilterOption({
    required this.id,
    required this.label,
    required this.color,
  });

  final String? id;
  final String label;
  final Color color;
}
